Drop the login background video here as `login_hero.mp4`.

The video should be short, silent (it plays muted anyway), and loop
cleanly — it's used as a looping background behind the login page's
branded panel.
