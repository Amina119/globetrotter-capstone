Drop place photos here, named to match the `imageAsset` path referenced in
`lib/data/sample_places.dart`, e.g. `assets/places/san_francisco/carrefour.jpg`.

Until a photo exists at its path, the place detail screen shows a colorful
icon placeholder instead — nothing breaks if images arrive later.
